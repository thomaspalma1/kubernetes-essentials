apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: webserver
  annotations:
    environment: qa
  name: webserver
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webserver
  strategy: {}
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - image: nginx
        name: nginx
        resources: {}
status: {}
